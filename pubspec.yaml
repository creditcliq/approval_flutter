name: approval_flutter
description: "A new Flutter package project."
version: 0.0.1
# homepage:

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: 4.13.0
  webview_flutter_android: ^4.10.5
  webview_flutter_wkwebview: ^3.23.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


# The following section is specific to Flutter packages.
# flutter:
